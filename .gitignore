# Local Claude Code settings (per-machine permissions)
.claude/settings.local.json

# Python bytecode cache
__pycache__/
*.pyc

# OS / app temp files
.DS_Store
Thumbs.db
desktop.ini

# Excel/Word lock files
~$*

# Data sources — kept local-only so the source platform identifiers
# never leave the dev machine. Only observations.json (the build output)
# gets committed.
*.xlsx
